---
---

<script>
  // Pagefind Component UI registers <pagefind-modal-trigger>, <pagefind-modal>,
  // and friends as custom elements. The runtime search index lives at
  // `/pagefind/`, generated by `npm run docs-build`.
  //
  // We deliberately skip `import '@pagefind/component-ui/css'` here — the
  // CSS is vendored into `site/src/scss/vendor/_pagefind-component-ui.scss`
  // so it ships through our normal SCSS pipeline (and is themable).
  import '@pagefind/component-ui'
  import stackblitz from '../assets/stackblitz.js'

  stackblitz()
</script>
