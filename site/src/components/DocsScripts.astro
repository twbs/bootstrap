---
---

<script>
  // We use `@pagefind/component-ui` purely for its JS API
  // (`getInstanceManager()`, search engine, translations, announcer). No
  // `<pagefind-*>` element is rendered anywhere in the docs — the search UI
  // is built from Bootstrap primitives in `assets/search.js` (custom
  // `<bd-search-input>` and `<bd-search-results>` elements driven by the
  // instance API). The runtime search index lives at `/pagefind/`,
  // generated by `npm run docs-build`.
  import '../assets/search.js'
  import stackblitz from '../assets/stackblitz.js'

  stackblitz()
</script>
