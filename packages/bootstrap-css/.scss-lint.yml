# Default application configuration that all configurations inherit from.
scss_files:
  - "src/**/*.scss"

plugin_directories: ['../../build']

# List of gem names to load custom linters from (make sure they are already
# installed)
plugin_gems: []

# Default severity of all linters.
severity: warning

linters:
  Comment:
    enabled: true
    exclude:
      - src/bootstrap.scss
    style: silent